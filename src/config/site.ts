export const siteConfig = {
  name: 'Git for Prompts',
  headline: 'The local-first prompt package manager.',
  tagline: 'Version, diff, and evaluate your AI prompts — offline first, cloud when you need it.',
  description: 'gfp is a developer-first CLI and web platform for versioning AI prompt bundles. Store locally in SQLite, sync to cloud when ready, run evals against any provider.',
  url: process.env.NEXT_PUBLIC_BASE_URL || process.env.NEXT_PUBLIC_APP_URL || 'https://gitforprompts.vercel.app',
};
